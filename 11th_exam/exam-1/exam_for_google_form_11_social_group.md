# Exam for Grade 11 Social Group  
**Duration:** 35 minutes  
**Difficulty:** Easy–Medium (mostly easy)  
**Based on:** `exam_1_for_11_social_group.md` (Theory: Composite Data Types)

---

## Variant 1

### Section 1: Multiple Choice (1 point each)

1. What does the term *data type* mean?  
   a) A way to store information in computer memory  
   b) A set of values and operations on them  
   c) Only numeric data format  
   d) A sorting method  

2. Which of the following is a **simple** data type?  
   a) Array  
   b) String  
   c) Integer  
   d) Record  

3. Which data type is used to store a sequence of characters?  
   a) int  
   b) float  
   c) string  
   d) bool  

4. What is an **array**?  
   a) A set of random numbers  
   b) A collection of homogeneous data elements arranged sequentially  
   c) A table with different data types  
   d) Only a two-dimensional structure  

5. What is the index of the first element in an array in C++?  
   a) 1  
   b) 0  
   c) -1  
   d) Depends on the data type  

6. Which data type is used to store truth values?  
   a) int  
   b) bool  
   c) char  
   d) string  

7. What does the function `strlen(S)` do?  
   a) Compares two strings  
   b) Returns the length of a string  
   c) Copies a string  
   d) Deletes a string  

8. Which symbol marks the end of a string in a character array?  
   a) `\n`  
   b) `\0`  
   c) `\t`  
   d) `\\`  

9. Which data type is used to store a set of values defined by identifiers?  
   a) enum  
   b) bool  
   c) string  
   d) float  

10. What does the function `strcat(S1, S2)` do?  
    a) Compares strings  
    b) Copies a string  
    c) Concatenates strings  
    d) Deletes a string  

---

### Section 2: True / False (1 point each)

11. An array can contain elements of different data types.  
12. A string of type `string` requires specifying the maximum length when declared.  
13. In C++, array indices start from zero.  
14. The `char` data type is used to store characters.  
15. The `cin.getline()` function allows reading a string with spaces.  

---

### Section 3: Short Answer (2 points each)

16. Give two examples of **simple data types**.  
17. Give two examples of **composite data types**.  
18. Explain the difference between simple and composite data types.  
19. What is **bubble sort** and how does it work?  
20. Which data type would you use to store monthly family electricity expenses? Explain why.  

---

### Section 4: Practical (3 points each)

21. Write a declaration of a one-dimensional array of 10 elements of type `float`.  
22. Write a code fragment that reads 5 integers into an array and prints their sum.  
23. Write a code fragment that combines two strings `FirstName` and `LastName` into one string `FullName` with a space between them.  

---

### Section 5: Bonus (5 points)

24. Write a program that reads a string and prints the number of spaces in it.  
*(Hint: use the `string` data type and a `for` loop.)*

---

**Total Points:** 40  
**Passing Score:** 24 (60%)  
**Time Limit:** 35 minutes  

---

## Variant 2

### Section 1: Multiple Choice (1 point each)

1. Which of the following is a **composite** data type?  
   a) int  
   b) float  
   c) array  
   d) bool  

2. What does the term *composite data* mean?  
   a) Data formed by combining simple data  
   b) Only numeric data  
   c) A programming language  
   d) A sorting algorithm  

3. Which data type is used to store real numbers?  
   a) int  
   b) char  
   c) float  
   d) enum  

4. What is the purpose of an index in an array?  
   a) To delete elements  
   b) To access specific elements  
   c) To sort the array  
   d) To count elements  

5. In C++, what is the last valid index of an array declared as `int A[10];`?  
   a) 10  
   b) 9  
   c) 11  
   d) 0  

6. Which data type is used to store single characters?  
   a) string  
   b) int  
   c) char  
   d) float  

7. What does the function `strcpy(S1, S2)` do?  
   a) Compares strings  
   b) Copies string S2 into S1  
   c) Concatenates strings  
   d) Returns string length  

8. Which header file is needed to use string functions like `strlen` and `strcpy`?  
   a) `<iostream>`  
   b) `<cstring>`  
   c) `<cmath>`  
   d) `<algorithm>`  

9. What is the `float` data type used for?  
   a) Storing integers  
   b) Storing real numbers  
   c) Storing characters  
   d) Storing truth values  

10. Which operator is used to concatenate two strings of type `string`?  
    a) `*`  
    b) `+`  
    c) `-`  
    d) `/`  

---

### Section 2: True / False (1 point each)

11. Simple data types include integer, float, char, and bool.  
12. Arrays in C++ can have elements of type string.  
13. The escape character `\0` marks the end of a character array string.  
14. Bubble sort is a method for searching elements in an array.  
15. The function `getline(cin, S)` reads a string including spaces.  

---

### Section 3: Short Answer (2 points each)

16. Give two examples of **composite data types**.  
17. What is the difference between `char` array strings and `string` type?  
18. Explain what an **array** is and give an example of its use.  
19. What is the purpose of the `\0` character in character arrays?  
20. Which data type would you use to store daily temperatures for a month? Explain why.  

---

### Section 4: Practical (3 points each)

21. Write a declaration of a one-dimensional array of 15 elements of type `int`.  
22. Write a code fragment that reads 3 real numbers into an array and calculates their average.  
23. Write a code fragment that reads two strings `Name` and `Surname` and displays them together.  

---

### Section 5: Bonus (5 points)

24. Write a program that reads a string and prints the number of letter 'a' occurrences in it (both uppercase and lowercase).  
*(Hint: use the `string` data type and check for both 'a' and 'A'.)*

---

**Total Points:** 40  
**Passing Score:** 24 (60%)  
**Time Limit:** 35 minutes  

---

## Variant 3

### Section 1: Multiple Choice (1 point each)

1. What are **simple data types**?  
   a) Data types that store multiple values  
   b) Data types that store a single value  
   c) Only arrays  
   d) Only strings  

2. Which of the following is NOT a simple data type?  
   a) int  
   b) string  
   c) bool  
   d) char  

3. Which data type stores whole numbers?  
   a) float  
   b) char  
   c) int  
   d) string  

4. What is the main characteristic of an array?  
   a) Elements can be of different types  
   b) All elements are of the same type  
   c) It can only store numbers  
   d) It has no indices  

5. How do you access the third element in array `V` in C++?  
   a) `V[3]`  
   b) `V[2]`  
   c) `V(3)`  
   d) `V(2)`  

6. Which data type is best for storing yes/no values?  
   a) int  
   b) float  
   c) bool  
   d) char  

7. What does the function `S.length()` return for a string `S`?  
   a) The first character  
   b) The number of characters  
   c) The last character  
   d) The string itself  

8. In C++, how do you declare a string variable using the string type?  
   a) `char S;`  
   b) `string S;`  
   c) `int S;`  
   d) `array S;`  

9. What is the `enum` data type used for?  
   a) Storing real numbers  
   b) Defining a set of named constants  
   c) Storing characters  
   d) Creating arrays  

10. Which function concatenates two character array strings?  
    a) `strlen`  
    b) `strcmp`  
    c) `strcat`  
    d) `strcpy`  

---

### Section 2: True / False (1 point each)

11. Composite data types are formed by combining simple data types.  
12. In C++, you must specify the maximum length when declaring a `string` variable.  
13. Array indices in C++ always start from 1.  
14. The `real` or `float` data type is used for decimal numbers.  
15. You can use `cin >>` to read a string with spaces.  

---

### Section 3: Short Answer (2 points each)

16. Give two examples of **simple data types**.  
17. Name three composite data types.  
18. Explain the purpose of arrays in programming.  
19. What sorting method is called "bubble sort"? Describe it briefly.  
20. Which data type would you use to store student names in a class? Explain why.  

---

### Section 4: Practical (3 points each)

21. Write a declaration of a one-dimensional array of 20 elements of type `char`.  
22. Write a code fragment that reads 4 integers into an array and finds the maximum value.  
23. Write a code fragment that declares a string variable and assigns it the value "Informatica".  

---

### Section 5: Bonus (5 points)

24. Write a program that reads a string and prints it in reverse order.  
*(Hint: use the `string` data type and access characters by index.)*

---

**Total Points:** 40  
**Passing Score:** 24 (60%)  
**Time Limit:** 35 minutes  

---

## Variant 4

### Section 1: Multiple Choice (1 point each)

1. Which statement about data types is correct?  
   a) All data types can store any kind of data  
   b) Each data type has specific values and operations  
   c) Data types are not important in programming  
   d) Only numbers need data types  

2. Which is an example of a **composite** data type?  
   a) char  
   b) int  
   c) array  
   d) bool  

3. What does `string` store?  
   a) A single character  
   b) A sequence of characters  
   c) Only numbers  
   d) Boolean values  

4. In a one-dimensional array, how are elements organized?  
   a) In a random order  
   b) In a sequence  
   c) In a tree structure  
   d) In a circular pattern  

5. What is the valid range of indices for array `int A[8];` in C++?  
   a) 1 to 8  
   b) 0 to 7  
   c) 0 to 8  
   d) 1 to 7  

6. Which data type would you use for storing a person's age?  
   a) string  
   b) bool  
   c) int  
   d) char  

7. What does `strcpy` function do?  
   a) Compares two strings  
   b) Finds string length  
   c) Copies one string to another  
   d) Concatenates strings  

8. Which character marks the end of a string in a character array?  
   a) Space  
   b) `\n`  
   c) `\0`  
   d) `\t`  

9. What is the `bool` data type used for?  
   a) Storing numbers  
   b) Storing true/false values  
   c) Storing text  
   d) Storing characters  

10. How do you combine strings `S1` and `S2` of type `string`?  
    a) `S1 - S2`  
    b) `S1 * S2`  
    c) `S1 + S2`  
    d) `S1 / S2`  

---

### Section 2: True / False (1 point each)

11. An array must contain elements of the same data type.  
12. The `string` type requires you to specify maximum length when declaring.  
13. In C++, the first element of an array has index 0.  
14. The `char` data type can store an entire word.  
15. The function `cin.getline()` can read strings containing spaces.  

---

### Section 3: Short Answer (2 points each)

16. List two simple data types and explain what each stores.  
17. List two composite data types.  
18. What is the main difference between simple and composite data?  
19. Describe what bubble sort does.  
20. Which data type would you use to store prices of items in a store? Explain.  

---

### Section 4: Practical (3 points each)

21. Write a declaration of a one-dimensional array of 12 elements of type `double`.  
22. Write a code fragment that reads 6 integers into an array and prints the smallest value.  
23. Write a code fragment using `string` type that stores your first name and last name in separate variables, then combines them.  

---

### Section 5: Bonus (5 points)

24. Write a program that reads a string and counts how many vowels (a, e, i, o, u) it contains.  
*(Hint: use the `string` data type and check each character.)*

---

**Total Points:** 40  
**Passing Score:** 24 (60%)  
**Time Limit:** 35 minutes  

---

## Variant 5

### Section 1: Multiple Choice (1 point each)

1. What does a data type define?  
   a) Only how data is displayed  
   b) The set of values and operations on them  
   c) Only the size of memory  
   d) The programming language  

2. Which of these is a **simple** data type?  
   a) array  
   b) record  
   c) float  
   d) string  

3. Which data type is for text sequences?  
   a) int  
   b) bool  
   c) string  
   d) float  

4. What describes an array best?  
   a) Unorganized collection of different data  
   b) Collection of same-type elements in sequence  
   c) A single value  
   d) A mathematical formula  

5. In C++, array indexing starts from which number?  
   a) 1  
   b) -1  
   c) 0  
   d) 10  

6. Which data type stores true or false?  
   a) char  
   b) int  
   c) bool  
   d) float  

7. What does `strlen(S)` return?  
   a) The first character of S  
   b) The number of characters in S  
   c) A copy of S  
   d) The last character of S  

8. What symbol ends a string in a char array?  
   a) `\n`  
   b) Space  
   c) `\0`  
   d) `\t`  

9. What is `enum` used for?  
   a) Storing decimal numbers  
   b) Defining named constant values  
   c) Creating arrays  
   d) Storing characters  

10. What does `strcat(S1, S2)` do?  
    a) Compares S1 and S2  
    b) Copies S2 to S1  
    c) Adds S2 to the end of S1  
    d) Deletes S2  

---

### Section 2: True / False (1 point each)

11. Arrays can contain elements of different types.  
12. The `string` type does not require specifying maximum length.  
13. Array indices in C++ start from zero.  
14. The `char` type stores individual characters.  
15. You cannot read strings with spaces using `cin >>`.  

---

### Section 3: Short Answer (2 points each)

16. Name two simple data types and briefly describe each.  
17. Name two composite data types.  
18. Explain how composite data differs from simple data.  
19. What is bubble sort and when is it used?  
20. Which data type would you use for storing weekly rainfall measurements? Explain.  

---

### Section 4: Practical (3 points each)

21. Write a declaration of a one-dimensional array of 25 elements of type `int`.  
22. Write a code fragment that reads 5 real numbers into an array and prints their product.  
23. Write a code fragment that creates a full name by combining `First` and `Last` string variables.  

---

### Section 5: Bonus (5 points)

24. Write a program that reads a string and checks if it is a palindrome (reads the same forwards and backwards).  
*(Hint: compare characters from both ends moving toward the middle.)*

---

**Total Points:** 40  
**Passing Score:** 24 (60%)  
**Time Limit:** 35 minutes  

---

**Note for Google Form Setup:**  
- Use **Multiple Choice** for Section 1.  
- Use **Checkbox (True/False)** for Section 2.  
- Use **Short Answer** for Section 3.  
- Use **Paragraph** for Section 4 and 5.  
- Assign variant randomly to ensure fairness.  
- Enable automatic grading for Sections 1–2 where possible.
- Each variant maintains similar difficulty while testing the same concepts.
